var ready;
ready = function() { 

	runClock();
	$(function(){
	       $("#typingEffect").typed({
	           strings: ["exam preparation?", "career advice?", "research?", "assignments?"],
	           typeSpeed: 150,
	           backDelay: 1000,
	           loop: true
	       });
	   });


	// Growl 2.0, need to be updated...logic is good and tested
	if ($('#flashNotice').data("notice")) {
		var notice = $('#flashNotice').data("notice")
		$.growl({
			title: "<strong>Testing: </strong>",
			message: notice,
			placement: {
					from: "top",
					align: "right"
				},
			offset: 20,
			spacing: 10,
			z_index: 1031,
			delay: 5000,
			timer: 1000
		});
	}



}
$(document).ready(ready);
$(document).on('page:load', ready);

function runClock(){
	t = setTimeout(function(){
		$('#timeNow').text(moment().format('MMMM Do YYYY, h:mm:ss a'));
		$('#hourNow').text(moment().format('dddd ha'));
		runClock();
	}, 1000)
}